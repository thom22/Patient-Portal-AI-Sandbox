# =============================================================================
# SmartRep AI Sandbox — requirements
# Tested with Python 3.10.6
# Install:  pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# Core (always required by app.py / synthetic_data.py)
# -----------------------------------------------------------------------------
streamlit==1.46.1
pandas==2.2.3
numpy==1.26.4           # pinned <2 to keep pandas 2.2.x ABI happy on 3.10

# -----------------------------------------------------------------------------
# Live mode — Azure OpenAI calls in ai_framework.py (lazy-imported)
# Omit these if you only ever run mode="simulation"
# -----------------------------------------------------------------------------
openai==2.14.0
tiktoken==0.9.0

# -----------------------------------------------------------------------------
# Analytics charts — plotly is lazy-imported in app.py around line 801
# -----------------------------------------------------------------------------
plotly==6.2.0

# -----------------------------------------------------------------------------
# OPTIONAL: spaCy NER de-identification
# Only needed if USE_SPACY=true in your .env
# After installing, also run:
#   python -m spacy download en_core_web_sm
# -----------------------------------------------------------------------------
spacy==3.8.7
python -m spacy download en_core_web_sm

# -----------------------------------------------------------------------------
# OPTIONAL: scispaCy biomedical NER for medication preservation
# Only needed if USE_SCISPACY=true. Install spacy first, then:
  pip install scispacy==0.5.4
  pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_ner_bc5cdr_md-0.5.4.tar.gz
# -----------------------------------------------------------------------------
# scispacy==0.5.4

# -----------------------------------------------------------------------------
# OPTIONAL: Faker — only used when DeIdentificationConfig.name_generation_method
# is set to "faker" (synthetic / coded / pattern modes don't need it)
# -----------------------------------------------------------------------------
# faker==37.4.0

# -----------------------------------------------------------------------------
# OPTIONAL: anthropic SDK — only if you swap providers in ai_framework.py
# -----------------------------------------------------------------------------
# anthropic==0.46.0